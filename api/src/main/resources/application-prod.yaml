spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    root: info

app:
  security:
    jwt:
      secret: ${JWT_SECRET}
  pago:
    base-url: ${PAGO_BASE_URL}
